import Screen from '@/components/Screen';
import { Text } from 'react-native';

interface IProps {}

const Home: React.FC<IProps> = () => {
  return (
    <Screen>
      <Text>Home</Text>
    </Screen>
  );
};

export default Home;
